---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="mb-8">
  <h2 class="mb-4 border-b border-bd pb-2 text-xl font-semibold">{title}</h2>
  <slot />
</section>
